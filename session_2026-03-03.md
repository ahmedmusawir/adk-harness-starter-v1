# Session Log: 2026-03-03

## Project Context

- **Project:** ADK Harness Starter v1
- **Tool:** Claude Code
- **Goal:** Pre-Phase-1A cleanup + skill file creation

## Starting State

- **Branch:** kit-handbook-v1
- **Last Working Feature:** All 3 existing agents working (product_agent_rico_1, jarvis_agent, ghl_mcp_agent)
- **Known Issues:** None — pre-Phase-1A cleanup was completed yesterday (2026-03-02)

## Session Progress

### 09:00 — Pre-Phase-1A Cleanup Execution (10 fixes)

- Resumed from previous session where plan had been researched but not yet executed
- Executed all 10 fixes: .env_example, Dockerfile, grant_permissions.sh, ghl_mcp_agent/agent.py (comment), jarvis_agent/agent.py (unused import), .gitignore, NAMING_CONVENTIONS.md, all APP_BRIEFS (shared→utils, token_tracker→token_calculator), repo name fixes across all APP_BRIEFS, created CHANGELOG.md
- Fix #9 scope expanded by Tony: repo name fix applied to ALL APP_BRIEFS (not just MASTER_BRIEF)

### 10:00 — GHL MCP Agent locationId Bug Fix

- User reported: ghl_mcp_agent was asking for locationId during conversation
- Diagnosis: GHL MCP tool schemas expose locationId as an explicit parameter; system prompt said "it's configured" but never told the LLM the actual value
- Fix: Changed `return """` to `return f"""` in `get_rico_instructions()` and injected `{GHL_LOCATION_ID}` directly into the prompt text so the LLM can fill the parameter without asking the user
- Root .env confirmed to have GHL_API_TOKEN and GHL_LOCATION_ID — env loading was not the issue

### 11:00 — SCAFFOLD_NEW_AGENT Skill File Created

- Task: create `skills/SCAFFOLD_NEW_AGENT.md` — a documentation-only skill file for scaffolding new agents
- Research: read all 3 existing agents + utils/gcs_utils.py to ground the skill in real patterns
- Key finding: product_agent_rico_1 has naming inconsistency (fetch_instructions("product_agent") vs folder "product_agent_rico_1") — flagged as bug-to-avoid in skill file
- Created `skills/` directory + `skills/SCAFFOLD_NEW_AGENT.md` (v1.0)
- 4 boilerplate patterns documented: standard GCS, GCS + context tool, env vars, google_search
- Updated CHANGELOG.md

## Lessons Learned

- GHL MCP tools require locationId as explicit parameter in tool schema (not just as HTTP header) — inject actual value into system prompt via f-string
- `product_agent_rico_1` fetch_instructions key ("product_agent") doesn't match folder name — is a pre-existing inconsistency, not the convention
- User is testing `gemini-3-flash-preview` model on jarvis_agent and product_agent_rico_1 (manually changed, not via Claude Code)

### 12:00 — SK-1 Token Calculator

- Created `utils/token_calculator.py` with 3 public functions: `count_tokens`, `estimate_cost`, `get_model_pricing`
- Verified Vertex AI token counting API: `client.models.count_tokens(model=..., contents=...)` → `CountTokensResponse.total_tokens`
- Created `tests/test_token_calculator.py` (10 tests: 7 unit, 3 integration), `tests/__init__.py`, `pytest.ini`
- All 10 tests green: `pytest tests/test_token_calculator.py -v`

### 13:00 — SK-2 Run Receipt

- Created `utils/run_receipt.py` — 3 public functions: `create_receipt`, `format_receipt`, `save_receipt_to_file`
- Created `tests/conftest.py` — 3 shared fixtures
- Created `tests/test_run_receipt.py` — 13 tests (8 integration, 5 unit), all green
- Full regression: `pytest tests/ -v` → 23/23 passed (SK-1 + SK-2)

### 15:00 — SK-INT Integration Wiring & Usage Reporting

- Created `callbacks/receipt_callback.py` — `get_start_time_callback()` (before_model) + `get_receipt_callback(agent_name, model)` (after_model)
- Wired callbacks into `product_agent_rico_1/agent.py` — only agent modified, no logic changes
- Created `scripts/usage_report.py` — stdlib-only CLI, `--date` arg, ASCII table output
- Created `tests/test_receipt_callback.py` — 6 unit tests, all mocked, all green
- Added `logs/` to `.gitignore`
- Full regression: `pytest tests/ -v` → 42/42 passed

### 14:00 — SK-4 Context Cache

- Completed Correction 3 research (live-tested): tools + cached_content CANNOT coexist in generate_content config (API 400). BUT tools CAN be baked into CreateCachedContentConfig — confirmed working.
- Created `utils/context_cache.py` with 5 public functions: `create_cache`, `get_cache`, `delete_cache`, `list_caches`, `estimate_cache_savings`
- Key corrections applied: discount rate = 10% of input (90% off); fixture = 350 reps ≈ 5,600 tokens; tools param on create_cache
- Created `tests/test_context_cache.py` — 13 tests (10 integration, 3 unit)
- Full regression: `pytest tests/ -v` → 36/36 passed (SK-1 + SK-2 + SK-4)
- Documented architectural decision in CHANGELOG.md under "Architectural Decision: Vertex Caching + Tools"

## End of Session State

- **Working:** All 3 existing agents; ghl_mcp_agent locationId bug fixed; SK-1, SK-2, SK-4 implemented and tested
- **Broken:** Nothing
- **Next Steps:** Phase 1A — read STARTER_KIT_SPEC.md then PHASE_1A_AGENT_SET_1_RICO_CACHING.md, enter Plan Mode before building

## Files Changed This Session

- `utils/context_cache.py` — created (SK-4)
- `tests/test_context_cache.py` — created (SK-4, 13 tests green)
- `CHANGELOG.md` — SK-4 entry + Architectural Decision: Vertex Caching + Tools
- `.env_example` — GOOGLE_GENAI_USE_VERTEXAI=1
- `Dockerfile` — python:3.12-slim
- `grant_permissions.sh` — added secretmanager role
- `ghl_mcp_agent/agent.py` — corrected header comment + f-string locationId fix
- `jarvis_agent/agent.py` — removed unused import
- `.gitignore` — ghl_mcp_agent/.env entry
- `docs/NAMING_CONVENTIONS.md` — GCS path schema updated
- `docs/APP_BRIEFS/MASTER_BRIEF_v1.2.md` — shared→utils, repo name
- `docs/APP_BRIEFS/PHASE_1_OVERVIEW.md` — shared→utils, token_tracker→token_calculator, repo name
- `docs/APP_BRIEFS/PHASE_1_TESTING_STRATEGY.md` — shared→utils, token_tracker→token_calculator
- `docs/APP_BRIEFS/STARTER_KIT_SPEC.md` — repo name
- `docs/APP_BRIEFS/PHASE_1A_AGENT_SET_1_RICO_CACHING.md` — repo name
- `CHANGELOG.md` — created + updated
- `skills/SCAFFOLD_NEW_AGENT.md` — created (new skill file)
- `utils/token_calculator.py` — created (SK-1)
- `tests/__init__.py` — created
- `tests/test_token_calculator.py` — created (SK-1, 10 tests green)
- `pytest.ini` — created (marker registration)

EXECUTION COMPLETE

CHANGES MADE:

- jarvis_agent/agent.py — import added, callbacks wired
- ghl_mcp_agent/agent.py — import added, callbacks wired  


THINGS I DIDN'T TOUCH:

- callbacks/receipt_callback.py — zero changes
- product_agent_rico_1/agent.py — already wired, untouched
- All tests — 42/42 green

All three agents now write receipts to their own files in logs/receipts/. After running any agent, python
scripts/usage_report.py will show the full table across all three.
